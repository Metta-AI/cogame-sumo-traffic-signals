## CI shard 4 of 4: the static gates — the manifest, the chrome and the event
## vocabulary. Fast, so it also carries the label and endcard scans.
{.warning[UnusedImport]: off.}
import
  test_signals_manifest,
  test_signals_viewer,
  test_signals_endcard_labels,
  test_signals_events
{.warning[UnusedImport]: on.}
